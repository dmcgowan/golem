// +build ignore
package runner

func CreateLibchanForwarder(address string) (LogForwarder, error) {
	// Dial endpoint
	// Start libchan protocol
	//dialFunc := func() (net.Conn, error) {
	//	return net.Dial(network, address)
	//}
	return nil, nil
}
